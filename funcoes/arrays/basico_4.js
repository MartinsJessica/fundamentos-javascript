//// a variavel fica mais flexível e vai ate o finaçl digitado

const numeros = [1, 2, 3, 4, 5, 6];

for (let i = 0; i < numeros.length; i++) { // a variavel fica mais flexível e vai ate o finaçl digitado
    console.log(i, numeros[i]);
}

for (let i = 1; i < numeros.length; i += 2) { // a variavel fica mais flexível e vai ate o finaçl digitado
    console.log(i, numeros[i]);
}