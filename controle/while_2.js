let quantidade = 0;

while (Math.random() < 0.9) {
    quantidade++;
}
console.log("Qtde: " + quantidade);
//sera gerado um numereo aleatorio de 0 a 9 Math.random
// ele vai executar de forma indeterminada de repetições em quanto for verdadeiro > 0.9, e quando for false irá parar.